import { StyledHeader, LogoText } from "./header.styled";

const Header: React.FC = () => {
  return (
    <StyledHeader>
      <LogoText href="/">FLOWLA SHOP</LogoText>
    </StyledHeader>
  )
}

export default Header;