const DEFAULT_FONT_SIZE = 16;

const typography = {
  fontFamily: {
    Avenir: "Avenir, sans-serif",
  },
  fontSize: {
    default: `${DEFAULT_FONT_SIZE}px`,
    xs: `${0.625 * DEFAULT_FONT_SIZE}px`,
    s: `${0.75 * DEFAULT_FONT_SIZE}px`,
    m: `${DEFAULT_FONT_SIZE}px`,
    l: `${1.25 * DEFAULT_FONT_SIZE}px`,
    xl: `${1.5 * DEFAULT_FONT_SIZE}px`,
    xxl: `${2 * DEFAULT_FONT_SIZE}px`,
  },
};

export default typography;
